---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: workloadtypeconfigs.workload-template.io
spec:
  group: workload-template.io
  names:
    kind: WorkloadTypeConfig
    listKind: WorkloadTypeConfigList
    plural: workloadtypeconfigs
    singular: workloadtypeconfig
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.group
      name: Group
      type: string
    - jsonPath: .spec.kind
      name: Kind
      type: string
    - jsonPath: .status.conditions[?(@.type=="CRDAvailable")].status
      name: CRD
      type: string
    - jsonPath: .status.conditions[?(@.type=="WatchActive")].status
      name: Watch
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              annotationPaths:
                description: |-
                  Explicit dot-separated paths where the profile annotation should be set.
                  For CRs without a standard PodTemplateSpec (e.g., InferenceService).
                  Each path is set directly as an annotation location.
                items:
                  type: string
                type: array
              group:
                description: |-
                  API group of the workload resource (e.g., "kubeflow.org", "batch").
                  Empty string means core API group.
                maxLength: 253
                type: string
              kind:
                description: Resource kind (e.g., "Notebook", "InferenceService",
                  "Job").
                maxLength: 63
                minLength: 1
                type: string
              knownContainerNames:
                description: |-
                  Container names this workload type typically creates.
                  Used by the profile controller to validate that a profile's container
                  targets (by name) are compatible with this workload type.
                  Empty means any container name is valid.
                items:
                  type: string
                type: array
              nativePropagation:
                description: |-
                  When true, the component controller for this workload type natively
                  propagates annotations from the CR to pods. WTO will not patch the
                  parent CR. The pod webhook remains the enforcement point regardless.
                type: boolean
              podTemplatePath:
                description: |-
                  Dot-separated path from the CR root to the PodTemplateSpec parent.
                  WTO navigates to <path>.metadata.annotations to propagate the profile annotation.
                  Examples: "spec.template" (Job, Notebook), "spec.pytorchReplicaSpecs.Worker.template" (PyTorchJob).
                  Mutually exclusive with annotationPaths for a given workload type — use annotationPaths
                  for CRs without a standard PodTemplateSpec.
                type: string
              resource:
                description: Plural lowercase resource name for RBAC and dynamic client
                  (e.g., "notebooks").
                maxLength: 63
                minLength: 1
                pattern: ^[a-z][a-z0-9]*$
                type: string
              version:
                description: API version (e.g., "v1", "v1beta1").
                maxLength: 63
                minLength: 1
                type: string
            required:
            - group
            - kind
            - resource
            - version
            type: object
            x-kubernetes-validations:
            - message: at least one of podTemplatePath or annotationPaths must be
                set
              rule: has(self.podTemplatePath) || (has(self.annotationPaths) && size(self.annotationPaths)
                > 0)
          status:
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGVK:
                description: Resolved GroupVersionKind at last reconciliation (e.g.,
                  "kubeflow.org/v1, Kind=Notebook").
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
